I'd like you to change the GrepVCL projects main form (@@MainForm_VCL.pas) to a more traditional VCL look and feel.

For instance, let's :

1. Change the $00F4F4ED background
2. Change all TTogleSwitch's for traditional check boxes
3. The Blue HeaderPanel ( $00975C2C ) is possibly too modern

-----------

# Observations
When testing I was searching through large codebases it took a matter of a number of minutes before I had found everything, but with 30 seconds I could see the files and lines I had been searching for. So what we need to add to Grep.Core is a way to stop the search cleanly and for the Search and Clear buttons to come back to enabled immediately upon stopping. 

## What we need:
- Add to Grep.Core a way to stop the search cleanly 

## When Starting the Search
- We need a Stop button and a progress bar to appear when we have started searching and to both disappear when we click Stop. 
- The stop button could go where the Clear button is now
- The progress bar should also disappear 

## When Search is Stopped
- When the Search is stopped then Search and Clear buttons to come back to enabled immediately.
- The Stop button should disappear
- The progress bar should also disappear 

## Important
- The TProgress bar should be Style := pbstMarquee. I have created one in the GrepVCL project
- All these UI changes are for both GrepVCL and GrepFMX 

----------

With the last fix you have broken the VCL and the FMX projects. 
When I click Search it remains and Found 0 files and does not find anything. Investigate and fix please

----------

While testing I noticed in the GrepVCL and the GrepFMX projects that when the results are shown in the TListView and the user clicks on them to see the actual lines then the spaces are not preserved in that view, so I should be seeing blocks of code with outdents, but it looks like the lines are being trimmed. I checked the GrepCLI project and the spaces have not been removed here. Investigate and fix please.

----------

While testing I noticed in the GrepVCL and the GrepFMX projects I noticed that if I specified a number > 10 for the context lines then I could not see the line highlighted anymore. 

- This was observable when I set context lines to 20. 
- What context lines should show is n lines above and n lines below
- The actual line should highlighted and when expanded on the initial display
  - If set to 10, then when expanded it should be capable of showing 21 lines, 
      - with the highlighted line in centre and 10 lines above and 10 below


----------

# Folders and Wildcards

- The changes would start with @Source/Grep.Core.pas
  - Changes also required for @Source/MainForm_VCL.pas and @Source/MainForm_FMX.pas


- In @Source/Grep.Core.pas FWildcards is a string and only accepts a single value
- In @Source/Grep.Core.pas Search is a method which is being passed a string (aFolder) and only accepts a single value

# Change needed
I actually want to pass a number of values for each:
- For wildcards, for example: FWildcards := '*.pas,*.sql ';
- for Search also: Grep.Search('d:\projects,d:\dbcr');

----------

# Opening a file at the line number

Currently when an match is clicked in the list view in either the GrepFMX or the GrepVCL projects a panel in opened and the line is displayed along with a specified number of lines above and below

Current behaviour for the TogglePanel in @Source/MainForm_VCL.pas and the DetailsPanel in @Source/MainForm_FMX.pas is that when the user clicks on this panel it disappears 

# Changes required

It is frustrating if the user wants to open file at the point this panel is open and he can see context, 

- The application should open the file at the line number to allow the user to take a look at the file in it's entirety. 
- So can you add an "Open" button to the top right of this panel that will open the file and go to the line number. 
- When opening, it should use whatever is defined in the OS for that file extension to be opened with and if nothing is defined it should use notepad

Please make these changes

----------

# Changing the look of GrepFMX

- I'd like you to change the GrepFMX projects main form (@Source/MainForm_FMX.pas) to a more traditional Windows look and feel.
- It needs to match as closely as is possible the look and feel of the GrepVCL projects main form (@Source/MainForm_VCL.pas)

## To be clear

- No tabbed interface, no page control. 
  - All controls to be on the main form
- Search section to the Top left
- Filter section to the Bottom left
- Results section to the Right
- Remove the colour for the HeaderPanel ( $00975C2C )
- Reduce the size of some of the components to make it fit into the same dimensions as found in @Source/MainForm_VCL.pas 
 
----------

# Broken functionality

When changing the look of GrepFMX on the last prompt you have broken the way MatchesListView and the DisplayPanel work

- MatchesListView reaches the far right hand edge and the bottom of the form; it should not do this. 
- When I click on a match in the MatchesListView list view the DisplayPanel does not appear

Please investigate and fix


----------

# Inconsistent DisplayPanel functionality

When a user clicks the MatchesListView the DisplayPanel should appear for the number of lines specified in the context lines value. When the user clicks inside the DisplayPanel the panel should disappear completely. Currently when the user clicks inside the DisplayPanel the panel is minimised to a few lines of context. This is incorrect behaviour.

## NOTE:

- This change is for the GrepFMX project only
- The GrepVCL project has this functionality working correctly if you wish to compare

Please investigate and fix
----------


# Incorrect file icon

I am searching with filename wildcards set to "*.pas,*.txt,*.sql" and I have noticed that between pas, sql and txt the file icon does not change. 

# What is required

- The file icon should be using the icon that the windows system holds for that file type. So for, example, for .pas it is a centurion helmet because that is the file icon for Delphi source code

## NOTE:

- This change is for the GrepFMX project only

Please investigate and fix
----------


# Incorrect file icon

I am searching with filename wildcards set to "*.pas,*.txt,*.sql" and I have noticed that the icon now correctly uses the shell icon for that file extension. However there is an issue

# What is required

- The icon when displayed on the list view looks blurry. I suspect this might be down to the size you are using. 
- If you need to use a smaller icon size to get a clearer icon it is preferable over a large blurry icon

## NOTE:

- This change is for the GrepFMX project only

Please investigate and fix
----------




