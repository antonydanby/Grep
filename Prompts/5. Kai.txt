# Change to MatchesList population

- Currently if there is 1,000 matches for 200 files, for example, the list will have 1,000 entries
- This is not optimal and makes it hard to read

## Changes needed

- In the example above the list should show  200 individual files. 
- If a file had 5 distinct matches then I would expect to see

| File                         | Matches | First match                         |
|------------------------------|---------|-------------------------------------|
| c:\example\filename.pas      | 5       | show the first match at this column |

- When a use clicks on the list item to open the DisplayPanel I would expect 2 new buttons and an additional label
- In the top right hand area of the panel I would expect to see a label (in bold) with 1/5 and a up and down button underneath
  - when the down button is pressed I would expect to see 2/5 etc.. all the way to 5/5
  - Naturally, a user cannot click up at 1/5 or down at 5/5
  - As the user select 1/5, 2/5, 3/5, 4/5 and 5/5 I expect the DisplayPanel to cycle through the 1st, 2nd, 3rd, 4th and 5th match for the file

## NOTE:

- This change is for the GrepFMX and the GrepVCL project 

Please investigate and make this change

----------

# A few minor adjustments to DisplayPanel

1. The button should align under each other neatly at the top right hand side of DisplayPanel

So:
	|-------|
	| Open  | 
	|-------|
	
           1/5
     
	|-------|
	| Up    | 
	|-------|
	|-------|
	| Down  | 
	|-------|	

2. The user should be able to use the windows up and down keys to move by 1
3. The user should be able to use PgUp to get to the top match and PgDn to get to the bottom match

## NOTE:

- This change is for the GrepFMX and the GrepVCL project 

Please investigate and make this change


----------

# A few changes to the Open, Up, Down buttons on the DisplayPanel

- On the Open button on the DisplayPanel, remove the word Open and instead use the image found at @Resources/open_with_32dp.png
- On the Up button on the DisplayPanel, remove the word Up and instead use the image found at @Resources/keyboard_arrow_up_32dp.png
- On the Down button on the DisplayPanel, remove the word Down and instead use the image found at @Resources/keyboard_arrow_down_32dp.png
- The user should be able to use the windows Ctrl-O keys to Open the file
- The buttons should still align under each other neatly at the top right hand side of DisplayPanel

So:
	|-------|
	| image | 
	|-------|
	
           1/5
     
	|-------|
	| image |
	|-------|
	|-------|
	| image |
	|-------|

## NOTE:

- This change is for the GrepFMX and the GrepVCL project 

Please investigate and make this change


----------

# Open, Up, Down button height on the DisplayPanel

- The Open, Up and Down button heights need amending. 
  - The image is being truncated. The height needs to be almost double what it is now
- You will, of course, need to move the buttons down to cater for this
- The open button will still remain at the same postion
  - The 1/5 label and the up, down buttons will need to be moved down
- The buttons should still align under each other neatly at the top right hand side of DisplayPanel

So:
	|-------|
	| image | 
	|-------|
	
           1/5
     
	|-------|
	| image |
	|-------|
	|-------|
	| image |
	|-------|

## NOTE:

- This change is for the GrepFMX and the GrepVCL project 

Please investigate and make this change

----------

# Open, Up, Down button height on the DisplayPanel

- Open, Up, Down button height on the DisplayPanel on the GrepVCL are still truncating
  - On the GrepFMX project these buttons are fine
  - I have observed that these buttons on GrepVCL are not centred and I believe this may be the issue
- I have made the versions of the images that are smaller for GrepVCL, should you need them
  - On the Open button on the DisplayPanel, instead use the image found at @Resources/open_with_24dp.png
  - On the Up button on the DisplayPanel, instead use the image found at @Resources/keyboard_arrow_up_24dp.png
  - On the Down button on the DisplayPanel, instead use the image found at @Resources/keyboard_arrow_down_24dp.png

## NOTE:

- This change is only for the GrepVCL as the buttons seem fine on the GrepFMX projects
- It might be an idea to see why this is

Please investigate and make this change

----------

# Open, Up, Down button height on the DisplayPanel

- You have broken the Up and Down keys for the DisplayPanel in the GrepVCL project 
  - Pressing Up,Down does not allow the user to go up and down through a files matches as it did before
- The user should not be able to Tab through the buttons in here 
  - TabStop should be False for all of them

## NOTE:

- This change is only for the GrepVCL as the buttons seem fine on the GrepFMX projects
- It might be an idea to see why this is

Please investigate and make this change

----------

# Error on other machines

- When I compile and run this code on another machine aside from this I am getting the following error:

Cannot create file
"C:\Users\QXZ6F6C\Resources\open_with_24dp.png". The
system cannot find the path specified

- The GrepVCL and GrepFMX projects should not attempt to open any config files or external png files
- I have looked at the code and the png images are being loaded like resources. This is wrong

Please investigate and fix

